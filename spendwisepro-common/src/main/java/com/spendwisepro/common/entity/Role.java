package com.spendwisepro.common.entity;

/**
 * The Role class is an enumeration that represents different roles in a system.
 * It defines three constants: {@code USER}, {@code MODERATOR}, and {@code ADMIN}.
 */
public enum Role {
    USER,
    MODERATOR,
    ADMIN
}
